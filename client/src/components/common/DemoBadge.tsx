import React from 'react';

// In Real-Data mode, demo badges are disabled as the application operates on real user-created records
export const DemoBadge: React.FC<{ size?: 'sm' | 'md' }> = () => {
  return null;
};
